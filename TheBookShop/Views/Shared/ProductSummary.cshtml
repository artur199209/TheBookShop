@model Product

<div class="card d-inline-block m-1 p-2">
    <a asp-action="ProductDetails" asp-area="" asp-controller="Product" asp-route-productId="@Model.ProductId">
    <div class="navbar-light">
        <h4>
            @Model.Title
        </h4>
        <br/>
        <span class="align-content-center">
            <img src="@Model.Image"/>
        </span>
        <br/>
        <span class="badge badge-pill badge-primary">
            @Model.Price.ToString("c")
        </span>
    </div>
    </a>
    <form id="@Model.ProductId" asp-action="AddToCart" asp-controller="Cart" method="post">
        <input type="hidden" name="productId" asp-for="@Model.ProductId"/>
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()"/>
        <div class="text-center">
            <span class="card-text">
                <button class="btn btn-success" type="submit">
                    <span class="fa fa-shopping-cart"></span>
                    Dodaj do koszyka
                </button>
            </span>
        </div>
    </form>
</div>